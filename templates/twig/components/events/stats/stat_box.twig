<div class="stat_box mb-4">
	<div class="card">
		<div class="card-header">
			{{ stat.name }}
		</div>
		<div class="card-body d-flex flex-row text-center justify-content-center">
			<div class="stat_count">{{ attribute(stat.counts, stat_to_show|default('lifetime')) }}</div>
		</div>
	</div>
</div>