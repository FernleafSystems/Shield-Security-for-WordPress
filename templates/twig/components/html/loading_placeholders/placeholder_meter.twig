<div class="progress-metercard progress-metercard-summary {{ vars.is_hero|default(false) ? 'progress-metercard-hero' : '' }}" data-meter_slug="{{ vars.meter_slug }}">
	{% if vars.is_hero|default(false) %}
		{% include '/components/html/loading_placeholders/progress_meter_hero.twig' %}
	{% else %}
		{% include '/components/html/loading_placeholders/progress_meter.twig' %}
	{% endif %}
</div>
