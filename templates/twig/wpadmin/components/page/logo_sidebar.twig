<div class="mt-3 mb-2 logo-container">
	<a href="{{ hrefs.plugin_home }}" class="page-header-item d-flex align-items-center gap-2" target="_blank">
		<img class="navbar-brand"
		     src="{{ imgs.logo_small }}"
		     id="navbar-bannerlogo"
		     alt="Shield Logo" />
		<span class="logo-text">
			<span class="logo-title">{{ strings.plugin_name }}</span>
			<span class="logo-subtitle">{{ strings.running_version|striptags }}</span>
		</span>
	</a>
</div>
