{% extends '/email/base_email.twig' %}

{% block email_body %}
	<p>
		{{ body.strings.requested }}
		<br />{{ body.strings.purpose }}
	</p>

	<p>{{ body.strings.click_confirm }}</p>

	<p><strong>{{ body.strings.same_browser_warning }}</strong></p>

	<p><a href="{{ body.hrefs.confirmation_link|raw }}">{{ body.hrefs.confirmation_link|raw }}</a></p>

	<p>{{ body.strings.reinstate_notice }}</p>

	<p>{{ body.strings.thank_you }}</p>
{% endblock %}
