{% extends "/notices/base-info.twig" %}

{% block notice_body %}
	<div class="row">
		<div class="col">
			<p>{{ strings.setup }}</p>
{% if notice_attributes.insideWizard == 0 %}
			<p><a href="{{ hrefs.wizard }}">{{ strings.launch }} &rarr;</a></p>
{% endif %}
		</div>
	</div>
{% endblock %}