<div class="row">
	<div class="col scan-item-history">
		<h3 class="h4 mb-3">File: <span style="font-family: monospace">{{ vars.path_fragment }}</span></h3>

		<h4 class="h6">{{ strings.info }}</h4>
		<ul style="list-style: inside disc">
			<li>{{ strings.file_full_path_label }}: <code>{{ vars.file_full_path }}</code></li>
			<li>{{ strings.file_status_label }}: {{ vars.file_status }}</li>
		</ul>

		<h4 class="h6">{{ strings.file_description }}</h4>
		<ul>
			{% for line in vars.file_description %}
				<li>{{ line|raw }}</li>
			{% endfor %}
		</ul>
		{% if flags.show_malai_status %}
			<h4 class="h6">{{ strings.heading_malai_status }}</h4>
			<ul>
				<li>{{ strings.malware_status_of }}: <code>{{ strings.malai_status }}</code></li>
				{% for line in strings.malai_status_notes %}
					<li>{{ strings.note }}: {{ line }}</li>
				{% endfor %}
			</ul>
		{% endif %}

		{% if vars.recommendations_lines is not empty %}
			<h4 class="h6">{{ strings.recommendations }}</h4>
			<ul>
				{% for line in vars.recommendations_lines %}
					<li>{{ line }}</li>
				{% endfor %}
			</ul>
		{% endif %}

	</div>
</div>
