<form class="form-horizontal" id="OptinForm" target="_blank"
	  action="https://app.getresponse.com/add_subscriber.html"
	  method="post">

	{{ icwp_macros.formInput_Text( 'name', vars.name, 'Your Name', vars.name ) }}
	{{ icwp_macros.formInput_Email( 'email', vars.user_email, 'Your Email',
		vars.user_email, 'We never SPAM and you can remove yourself at any time.' ) }}
	{{ icwp_macros.formInput_Checkbox(
		'webform[consent#BbEO-ver#Syac]', 'true', 'Agree To Privacy Policy',
		strings.privacy_policy,
		'We treat your membership information under our strict, and GDPR-compliant, privacy policy.',
		'OptinConsent'
	) }}
	{{ icwp_macros.formInput_Hidden( 'campaign_token', 'agIJR' ) }}
	{{ icwp_macros.formInput_Hidden( 'start_day', '0' ) }}
	{{ icwp_macros.formInput_Hidden( 'thankyou_url', 'https://onedollarplugin.com/emails/confirm-shield-security/' ) }}
	{{ icwp_macros.formInput_Submit( 'Sign-Up', 'subscribe' ) }}
</form>

<script type="text/javascript">
	jQuery( document ).ready( function ( $ ) {
		var $oSubButton = $( 'form#OptinForm button' );
		var $oCheck = $( '#OptinConsent' );
		$oSubButton.attr( "disabled", "disabled" );
		$( document ).on( 'change', $oCheck, function () {
			$oSubButton.prop( "disabled", !$oCheck.is( ":checked" ) );
		} );
	} );
</script>