# PHP Matrix Configuration
# Used by local test scripts, Docker builds, and workflow helper scripts.
#
# GitHub workflow jobs may still pin explicit lanes when the workflow needs
# fixed minimum/latest coverage. Keep those lanes consistent with this file.

PHP_VERSIONS="8.2 8.3 8.4"
DEFAULT_PHP="8.2"
