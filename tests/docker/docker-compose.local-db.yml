x-shield-container-labels: &shield-container-labels
  com.fernleaf.harness: ${SHIELD_DOCKER_LABEL_HARNESS:-shield-plugin-integration-local}
  com.fernleaf.run-id: ${SHIELD_DOCKER_CONTAINER_RUN_ID:-manual}
  com.fernleaf.lane: ${SHIELD_DOCKER_LABEL_LANE:-integration-local}
  com.fernleaf.lifecycle: ${SHIELD_DOCKER_CONTAINER_LIFECYCLE:-reusable}
  com.fernleaf.expires-at: ${SHIELD_DOCKER_CONTAINER_EXPIRES_AT:-1970-01-01T00:00:00+00:00}

x-shield-network-labels: &shield-network-labels
  com.fernleaf.harness: ${SHIELD_DOCKER_LABEL_HARNESS:-shield-plugin-integration-local}
  com.fernleaf.run-id: ${SHIELD_DOCKER_CONTAINER_RUN_ID:-manual}
  com.fernleaf.lane: ${SHIELD_DOCKER_LABEL_LANE:-integration-local}
  com.fernleaf.lifecycle: ${SHIELD_DOCKER_CONTAINER_LIFECYCLE:-reusable}
  com.fernleaf.expires-at: ${SHIELD_DOCKER_CONTAINER_EXPIRES_AT:-1970-01-01T00:00:00+00:00}

services:
  mysql-local:
    image: ${SHIELD_INTEGRATION_DB_IMAGE:-mysql:8.0}
    labels: *shield-container-labels
    environment:
      MYSQL_ROOT_PASSWORD: testpass
      MYSQL_DATABASE: wordpress_test_local
    command: ${SHIELD_INTEGRATION_DB_COMMAND:---default-authentication-plugin=mysql_native_password --bind-address=0.0.0.0} --skip-log-bin
    tmpfs:
      - /var/lib/mysql:size=2G
    ports:
      - "3311:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "--protocol=tcp", "-h", "127.0.0.1", "-uroot", "-ptestpass"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 10s

networks:
  default:
    labels: *shield-network-labels
